---
name: 存储观察站
version: 1.0.0-beta2
description: >
  面向存储空间的快照差异与可视化诊断 skill。只要用户提到硬盘空间、存储变化、磁盘快满、
  1TB 容量对不上、多个用户目录、可视化存储报告、快照差异或想调试存储 dashboard，
  就使用本 skill。每次触发都自动执行一次只读快照，展示容量压力、APFS 物理盘/容器口径、
  所有本机用户一级目录、Top 占用、目录增减和手动快照历史，并生成单文件 HTML 报告。
  当前使用 Lieflat Charts Wire 的黑灰底，并用梅子绿/橙色承载好坏变化语义，专门用于调试可视化信息密度；不要调用
  原来的“存储分析器”来代替本 skill，也不要创建每日自动任务。
compatibility: Python 3 标准库；报告层跨平台，扫描与文件管理器动作按操作系统适配
---

# 存储观察站

这是一个独立调试中的跨平台存储可视化 skill。HTML 报告、快照格式、差异分析、数据口径和图表逻辑在所有平台共用；只有磁盘扫描、打开文件管理器和移入回收站通过平台适配层实现。它不覆盖、不修改原有 `存储分析器`；所有快照、JSON、HTML 和调试记录都写入指定的独立工作区。

## 触发后的固定动作

每次本 skill 被启用，先运行一次只读快照，不要求用户再说“执行快照”，也不设置每日或周期任务：

```bash
REPO_ROOT=/path/to/storage-cleanup-visual
WORKSPACE="$REPO_ROOT/.storage-observatory-workspace"
python3 "$REPO_ROOT/scripts/run_snapshot.py" \
  --workspace "$WORKSPACE"
```

快照完成后，必须立即启动受保护的本地报告服务；这是每次启用 skill 的默认动作，不需要用户再手动打开报告：

```bash
python3 "$REPO_ROOT/scripts/server.py" \
  --workspace "$WORKSPACE"
```

服务会选择可用的本机端口，在首页确认返回 `200` 后自动调用默认浏览器打开新地址。必须保持服务进程运行，并把命令输出的当前 `http://127.0.0.1:<port>/` 地址作为本次报告链接；不能只返回静态 HTML 路径，也不能复用上一次已经退出的端口。只有在系统无法调用浏览器时，才把下面的静态报告作为只读回退：

```text
"$WORKSPACE/outputs/storage-observatory-report.html"
```

扫描结束后必须先检查 `permission_notice.must_show_user`。只要它为 `true`，在回复用户时必须明确指出“扫描已完成，但有部分目录未完整读取”，同时给出当前平台、受限数量、是否有超时，以及对应的授权/修复步骤；这段说明必须出现在报告链接之前，不能只埋在 HTML 权限面板或 JSON 中。若为 `false`，不要无故提示权限问题。

扫描本身只读，不会在触发 skill 时删除、移动、清空废纸篓、改权限或自动清理。报告中的清理决策清单保留人工确认后的操作按钮；默认启动本地服务只是为了打开报告和恢复按钮能力，任何文件动作仍必须由用户在页面中逐次确认。

每次快照并行执行两类互不依赖的只读检查：目录核算回答“空间在哪里”，已知清理规则回答“哪些可再生缓存路径此刻真实存在”。即使深层 `du` 扫描超时或部分目录受权限保护，已知缓存路径仍会逐项测量并生成当前清理清单；禁止沿用旧报告中的绿色、黄色或红色项目。只有本次存在、测量成功、不是符号链接且达到最小体积的精确路径，才可进入绿色操作白名单。

## 默认浏览器预览与本地操作

固定动作已默认启动受保护的本地服务；单独调试或服务退出后需要恢复时，使用同一命令重新启动：

```bash
python3 "$REPO_ROOT/scripts/server.py" \
  --workspace "$WORKSPACE"
```

服务内部会先检查首页返回 `200` 再调用默认浏览器；回复用户前仍应确认当前地址有效。如果旧端口返回 `not found`、连接失败或服务已退出，必须重新启动本地服务并只打开新输出的有效地址，不能继续复用旧链接。

静态 HTML 仍显示按钮但保持禁用，并提示启动服务；本地服务会为当前会话注入随机 token，服务只监听 `127.0.0.1`，按钮请求还会经过路径白名单和 `$HOME` 越界检查。

- 绿色“纯缓存”：恢复“移到废纸篓”和“直接删除（不可逆）”。
- 黄色“用户数据”：只恢复“打开位置”，默认不提供批量删除；用户应先检查内容与备份。
- 红色“正规卸载”：只提供“打开位置（去卸载）”，不直接删除应用。

每次操作仍需在页面逐次确认；服务不会自动清理，也不会修改原来的“存储分析器”。服务停止后，已打开页面中的按钮请求会失效。

## 平台适配

- **macOS**：读取 APFS/文件系统和物理盘布局，使用 `du`、`diskutil` 等系统能力；移入废纸篓由访达处理。
- **Windows**：读取所有盘符、用户目录、AppData、临时目录和 Program Files；移入回收站使用 Windows Shell；打开位置使用资源管理器。
- **Linux / 其他 Unix**：使用 Python 标准库扫描根目录、用户目录、缓存和开发目录；打开位置使用 `xdg-open`，移入回收站遵循 XDG Trash 规范。

平台差异只影响原始数据采集和本地文件动作，不改变报告 HTML、快照 JSON、容量变化算法、图表或颜色语义。扫描仍然只读；只有用户在本地服务页面明确确认后，平台适配器才执行“打开位置”或“移入回收站”。

macOS 扫描采用并行的逐目录限时策略：受隐私保护或异常缓慢的路径会进入“权限/未展开”差额并记录扫描警告，不会阻塞整个 skill；快照入口另有总时限，超时也会返回最新的容量摘要与可用空间，而不是让调用失败。

## 权限受限时的明确指引

报告检测到拒绝读取、缓存路径无法完整测量或扫描超时时，必须在“清理决策清单”之前显示与当前平台匹配的权限面板，并列出受限路径示例。没有异常时不显示该面板。

- **macOS**：打开“系统设置 → 隐私与安全性 → 完全磁盘访问权限”，允许 Codex；若从终端执行，也允许 Terminal 或实际终端应用。完全退出并重开 Codex 后重新启用本 skill。若“移到废纸篓”失败，再在“隐私与安全性 → 自动化”中允许 Codex 或终端控制访达。
- **Windows**：先检查“Windows 安全中心 → 病毒和威胁防护 → 勒索软件防护 → 受控文件夹访问”。如被拦截，将实际执行的 Codex 或 Python 程序加入允许列表后重新快照。仅查看其他用户或系统级目录时才考虑管理员身份；普通用户缓存扫描不应默认要求管理员权限。
- **Linux / Unix**：用目标文件所属的桌面账号运行，检查目录所有权与 ACL；不要为了生成报告而用 `sudo` 启动本地操作服务。

权限仅用于扩大只读统计覆盖率，不能自动改权限，也不能扩大删除范围。删除与移入回收站仍受“本次规则命中 + 路径存在 + 非符号链接 + 当前用户目录 + 精确白名单”共同限制。

## 数据口径

- 容量展示统一使用十进制 `GB/TB`（1 TB = 1000 GB）；原始 `du` 的 KiB 数值先按 1024 字节换算为十进制 GB。禁止把 926.4 GiB 标成 926.4 GB；这正是 994.7 GB 被误显示为 926.4 GB 的原因。
- `system.disk_total/used/free` 是根文件系统所在 APFS 容器的文件系统口径；“本机目录与空间构成”使用不重叠的根目录口径与未使用空间对账，而不是把 APFS firmlink 重复相加。
- 报告同时展示物理盘、APFS 容器、系统/Recovery/Preboot 等结构；不要把 macOS 显示的 GiB 误写成物理盘标称 TB。
- `/Users` 做一级覆盖，至少展示当前用户、其他用户、Guest、root、Shared 和可见目录；当前用户再做 Library、Caches、Containers、Application Support、Downloads 和开发缓存的深度扫描。
- 根目录扫描必须纳入 `/Applications`、`/Users`、`/Library`、`/System`、`/private`、`/opt`、`/usr` 等同级路径。macOS 的 `/System/Volumes/Data` 与这些用户可见路径存在 APFS firmlink，计算 `/System` 时必须排除 `Data`，避免把同一空间算两次。
- 当前用户容量按 HOME 下所有可见一级子项的 `du` 合计，报告显示精确到 0.01 GB 的“可见合计”，不再写“至少”；权限拒绝路径仍在扫描数据中保留。
- 目录构成模块必须补上“系统/权限未展开”差额与“未使用空间”，并显示“总容量”和“已核算”两个核对值；根目录可见项、差额和未使用空间应合计到 `system.disk_total`。
- 权限不足必须单独列出。目录首次被纳入统计时只能标为“新增覆盖”，不能伪造成从 0 GB 增长。
- 缺失数值显示 `—`，禁止出现 `NaN`、`Infinity`、`undefined` 或把解析失败静默当成 0。

## 可视化交付契约

报告必须用结论优先的可视化组合，而不是把更多文字堆在页面上。至少保留并持续调试以下独立视图：

1. 已用空间与使用率：大数字 + F11 Tick Gauge 语义；剩余容量使用 `#43593B`，仪表盘标题为“已用空间”，不额外显示“剩余”标签。
2. 前后容量变化：只保留已用空间绝对值和自动缩放的变化轴；上次为灰色实心，本次按好坏变化着色。
3. 本机目录与空间构成：用总容量为基准的真实大小比例并列条，纳入 Applications、Users、Library、System 等所有根目录，并把系统/权限差额和未使用空间单列；所有条目（包括差额与未使用空间）统一按容量降序排列，确保可与总容量对账。

本机目录条的填充色按最终显示顺序动态分配灰阶：容量最大的中性条最深，随后从上到下逐级变浅，颜色深浅与容量降序保持一致；“系统/权限未展开”也作为中性灰条纳入连续灰阶，但不改变容量排序；未使用空间继续使用好变化色，不参与灰阶排名。
4. Top 5 占用：大小、占已用比例、类型、名称、绝对路径和内容说明。
5. 目录级增减：G10 Diverging Bar 语义；正负方向、阈值和“新增覆盖路径”状态必须写清楚。增加数值放在正向柱体右侧，减少数值放在负向柱体左侧，均与柱体垂直居中；数值字号按柱体长度动态缩放并保持最小可读尺寸，不能放到柱体上方或压到目录名称上。
6. 手动快照历史：F2 Hairline Line 语义，每个点对应真实快照，不插值、不自动补点。
   趋势图折线与节点动效总时长为原方案的一半（约 2.1 秒），保持从左到右的绘制顺序。
图型结构优先参考 Lieflat Charts 的真实 gallery：F11、F12、G10、F2；不要用普通图表库默认样式，也不要为了“更丰富”重复同一个结论。可视化调试优先增加新的数据维度或层级，而不是增加装饰。

## 当前色系与语义

以 Lieflat Charts `Wire`（编辑部红）为底，但按用户指定增加语义色：

- 背景：`#F0F0EE`
- 数据主体：`#22211F`、`#8F8E86`、`#C0BFB7`
- 好变化：`#43593B`，表示可用空间增加、已用空间减少、目录占用减少和剩余容量。
- 坏变化：`#F5572F`，表示可用空间减少、已用空间增加和目录占用增加。
- 变化图的方向词“减少”使用 `#43593B`，“增加”使用 `#F5572F`；左侧“减少”箭头使用 `#43593B`，右侧“增加”箭头使用 `#F5572F`；“占用”和其他中文标签保持中性黑色。
- 变化图两侧方向标题以中间竖轴为对称轴：左侧标题右边缘与右侧标题左边缘使用相同间距，不能因文字锚点不同而偏向一侧。
- 存储空间变化趋势中，折线保持黑色；第一点的圆点与具体已用空间数值同为中性黑色，后续每个圆点与其变化数字使用同一语义色：增加为 `#F5572F`，减少为 `#43593B`，无变化为黑色。后五个节点的圆点半径按绝对变化量相对缩放，最大半径固定为 7，不得超过当前第六个点的原有尺寸。

图表网格固定为两行两列：第一行左侧为“现在距离满盘还有多远”，右侧为正方形“内存透镜”；第二行左侧为正方形“存储空间变化趋势”，右侧为“变化落在哪些目录”。趋势图使用方形 viewBox 重新布局坐标，不得通过非等比拉伸改变比例。内存透镜引线从标签侧读作“先横线、再斜线”进入扇区；横向段要有足够长度，斜线保持克制；横线应与目录名处于同一视觉行，不能落在目录名和容量占比之间；横线末端按目录名实际宽度动态留出间距，不能穿过文字。标签不能固定在对称列上，而要按扇区象限、锚点距离和同侧密度动态排布：左上密集区的标签向上抬高，左下/第三象限的标签放在锚点下方，使引线从左下向右上进入饼图；右侧同理按象限避让。文字不必与另一侧标签纵向对齐。

黑灰承载容量、目录和历史数据；颜色只给数字、点、线和条等数据图形，不给中文标签上色。不得再引入其他预设的颜色；用户明确选择新色系时，才更新整份 HTML 的全局色系。

清理决策清单中的中性比例横条必须适配暗色模式：横条只保留单色填充、取消第二种底轨，并且所有条右端对齐；暗色模式切换为适配背景的浅灰填充。横条不使用好坏语义色。

清理决策清单比例横条按每个清理类别内部的最大项目缩放，并在提示中注明“相对本组最大占用”；不要用跨类别最大值压缩小缓存条，避免 2 GB 与数百 MB 因最小可见宽度而看起来相同。横条只显示一种中性灰色填充，不显示第二种灰色底轨；横条列和数值列固定宽度，所有条右端对齐；条长本身表达相对占用比例，暗色模式只切换为适配背景的浅灰填充。

清理决策清单的每个类别内部都按项目实际占用从大到小排序；横条长度按同一类别的最大项目动态缩放，第一项最长，后续项目依次按比例缩短。

内存透镜的引线必须使用对应饼图扇区的同一颜色值，保持线与扇区一一对应；不额外显示引线端点圆点；目录名称和数值文字继续使用中性文字色，不随扇区颜色改变。

内存透镜的目录名称和数值不要求同时出现：每组文字应在对应扇区/引线之后以小幅错峰显示，保持阅读顺序清晰；文字内容和颜色不变。

内存透镜保持板块尺寸不变，只调整板块内部比例：饼图半径按用户反馈相对放大时，不能同步放大卡片；当前布局在原 136 的基础上放大 20% 后再放大 10%，使用约 179。饼图到折点的横线保持原间距 30，折点到标签的斜线按 0.85 倍缩短约 15%，并同步微调标签位置以保持连接，不改变扇区、文字和颜色逻辑。

所有图表动效使用一次性视口懒加载：首屏只为当前可见的 `.reveal` 元素启动动画，滚动进入视口的新元素才启动；元素启动后立即标记为已加载并解除观察，上下滚动不得重复播放。若浏览器不支持 `IntersectionObserver`，回退为一次性加载全部动效；`prefers-reduced-motion` 仍然优先降级。F11 Tick Gauge 沿用 lieflat-charts 原版：100 条刻度按弧线顺序逐枚淡入，使用确定性刻度长度和轻微错峰，不改为整圈裁剪扫掠。

报告初始化分三阶段：先同步写入首屏骨架、摘要和空图表容器；首次绘制完成后，按视口可见性和图表优先级逐个放入空闲队列创建 SVG 内容；清理决策清单、目录构成和其他屏幕外模块在浏览器空闲时间或接近视口时插入。每个图表和每个屏幕外模块只初始化一次，不能因为滚动或重复观察而重新生成或重复播放动画。首屏仪表盘的 100 条刻度必须通过少量路径按帧增量绘制，不能在同一同步任务中一次性创建 100 个独立节点。

刷新与懒加载必须保持阅读位置：页面使用当前路径与查询参数作为会话内滚动位置键，刷新前记录 `scrollY`，关闭浏览器原生自动恢复，并在屏幕外内容插入完成后一次性恢复位置；不能因为首屏骨架高度暂时不足而把用户从清理清单或目录构成跳回趋势图。

首屏性能要求：首帧只负责结构、摘要和图表空壳；仪表盘优先，内存透镜及其他图表随后逐个初始化，不能在同一帧同时启动多个重绘密集型图表。内存透镜的引线布局不得在循环中逐条读取 SVG 实际尺寸；应使用画布文字测量或批量测量，避免同步布局抖动。F11 的 100 条刻度虽然各自拥有淡入节点，但必须在首帧之后分批初始化，避免阻塞首屏。

动效速度不能全部相同：目录构成条和目录变化条按可视长度使用次线性时长，并配合快速启动、缓慢收束的缓动；存储空间变化趋势的每段折线按几何长度分配时长，长段与短段的绘制速度应有可见差异，节点沿对应折线段完成后出现；F11 刻度采用原版的顺序淡入和轻微错峰，保留每个刻度长度的确定性变化。上述差异必须通过 CSS 时长、已有空闲队列和有限的轻量 SVG 节点实现，不得增加逐帧创建大量 DOM、同步测量或长任务。

“存储空间变化趋势”的当前动画方案已确认固定：保持约 2.1 秒总时长、按线段几何长度的次线性时长分配、`cubic-bezier(.22,.82,.35,1)` 绘制缓动，以及节点在对应线段完成后出现。后续只调整其他图表，不改变这组趋势动效参数，除非用户明确要求重新设计。

“现在，最大的占用在哪里？”表格的每一行都提供“一键打开”按钮，目标为该行路径；按钮通过本地服务白名单执行，静态文件打开时保持禁用，不能绕过路径校验。

## 失败与调试

如果快照失败，先保留已有报告和快照，报告错误原因，再执行一次原始只读扫描；禁止用空数据覆盖旧报告。生成后检查 HTML 源码和可见文本，确保没有 `NaN`、旧时间戳或旧可用空间数字；抽取 `<script>` 后运行 `node --check`。

调试工作区的评估提示保存在工作区的 `evals/evals.json`。新的视觉反馈优先记录在工作区，不要回写原 skill。
